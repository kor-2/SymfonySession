{% extends 'base.html.twig' %}

{% block title %}Liste des sessions{% endblock %}

{% block body %}
<h1><i class="fa-solid fa-rectangle-list fa-lg"></i> Liste des Sessions</h1>

<table class="table table-striped align-middle">
    <thead>
        <tr class="text-center">
            <th>Nom</th>
            <th>Date de début</th>
            <th>Date de fin</th>
            <th>Formation</th>
            <th>Nombre de places</th>
            <th>Places restantes</th>
            <th>Places prises</th>
            <th>Détails programme</th>
        </tr>
    </thead>
    <tbody>

        {% for programme in programmes %}
        {{ dump(programme) }}
            
        <tr class="text-center">
            <td>{{ programme.session.nom }}</td>
            <td>{{ programme.session.debut | date('d/m/y') }}</td>
            <td>{{ programme.session.fin | date('d/m/y') }}</td>
            <td>{{ programme.session.formation }}</td>
            <td>{{ programme.session.NbPlaces }}</td>
            <td>{{ programme.session.NbPlaces - programme.session.stagiaire | length}}</td>
            <td>{{ programme.session.stagiaire | length }}</td>
            <td><a class="btn btn-primary" href="{{ path('show_programme', {'id' : programme.id}) }}"><i class="fa-solid fa-rectangle-list"></i></a></td>
        </tr>
        
        {% endfor %}
    </tbody>
</table>
{% endblock %}
